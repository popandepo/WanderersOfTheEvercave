using UnityEngine;

namespace Code.AStarCode
{
    public class AStar : MonoBehaviour
    {
        public AStarHolder AStarHolder = new AStarHolder();

        // Start is called before the first frame update
        private void Start()
        {
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}